import React, { Component } from 'react'

class Profile extends Component {

    //You will be fetching the information from the api.

    render() {
        //create a var to display the users info.
        return (            
            <section className="">
                <h1>Profile</h1>
                {/* display users info. */}
            </section>
        );
    }
}

export default Profile